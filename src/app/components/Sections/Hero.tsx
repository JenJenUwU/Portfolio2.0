import React from "react";
import { DecoderText } from "../DecoderText";

const Hero = () => {
  return (
    <section>
      <DecoderText text="Hello, World!" delay={300} className="items-center" />
    </section>
  );
};
export default Hero;
